{
  "schemaVersion": 1,
  "entityId": "secretBackend",
  "paths": [
    "backend/n4x/secrets/backends.py",
    "backend/n4x/secrets/paths.py"
  ],
  "confidence": "high",
  "notes": {
    "responsibility": "Secret values stay outside the graph. Default is macos_keychain on Darwin, encrypted_local elsewhere. N4X_SECRETS_MASTER_KEY is required for encrypted_local in production."
  }
}
