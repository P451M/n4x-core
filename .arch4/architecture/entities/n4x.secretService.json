{
  "schemaVersion": 1,
  "entityId": "n4x.secretService",
  "paths": [
    "backend/n4x/secrets/**"
  ],
  "confidence": "high",
  "openQuestions": [
    "EncryptedLocalFileBackend still defaults to ~/Library/Application Support/N4X; runtime-root and N4X_SECRETS_MASTER_KEY are planned, not implemented."
  ],
  "notes": {
    "responsibility": "SecretReference and CredentialRecord metadata in the graph; secret values stay in macOS Keychain, encrypted local file, or memory."
  }
}
