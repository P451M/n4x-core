{
  "schemaVersion": 1,
  "entityId": "n4x.secretService",
  "paths": [
    "backend/n4x/secrets/**"
  ],
  "confidence": "high",
  "notes": {
    "responsibility": "SecretReference and CredentialRecord metadata in the graph; values stay in macos_keychain, encrypted_local, or memory. Default is Keychain on Darwin, encrypted file elsewhere. N4X_SECRETS_MASTER_KEY is required for encrypted_local in production. Storage root is N4X_RUNTIME_ROOT/config."
  }
}
