{
  "schemaVersion": 1,
  "entityId": "n4x.packageService",
  "paths": [
    "backend/n4x/system/packages.py",
    "backend/n4x/contracts/package.py"
  ],
  "confidence": "high",
  "notes": {
    "responsibility": "n4x.package.v2 export/import of Applications and Experiences. HTTP PUT /packages stages bytes only; import is a separate MCP step. delete_working_set exports include_data=true then deletes the subgraph. Definition-only v4 import into v5 is allowed; v4 with data is rejected. Host never auto-installs Office."
  }
}
