# Architecture decision records

ADRs record *why* a boundary exists and which alternatives were rejected.
[`SPEC.md`](../../SPEC.md) records *what is*. Public `/docs` on n4x-www does
not publish these files.
